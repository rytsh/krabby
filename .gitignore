bin/
dist/
krabby.yaml
krabby.toml
.env
.env.local

# Web UI build artifacts. web/dist is generated by `make ui` and embedded at
# build time; keep a placeholder so the go:embed always has something to embed.
_ui/node_modules/
web/dist/*
!web/dist/.gitkeep
